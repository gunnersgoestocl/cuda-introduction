float ff_weights1[768][768] = {
    // Initialize with random values or specific weights as needed
    {0.1, 0.2, 0.3, /* ... */, 0.768}, // Example row
    // Add more rows as necessary
};

void save_ff_weights1() {
    FILE *file = fopen("data/ff_weights1.bin", "wb");
    if (file) {
        fwrite(ff_weights1, sizeof(ff_weights1), 1, file);
        fclose(file);
    } else {
        // Handle file opening error
    }
}

int main() {
    save_ff_weights1();
    return 0;
}